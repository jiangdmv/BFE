

var minimumCardPick = function (cards) {
  if (cards.length < 2) {
    return -1;
  }
  arr = [];
  arr.push(cards[0]);
  length=-1

  for (let i=1; i<cards.length, i++){
    arr.push(cards[i]);
    
  }


};
